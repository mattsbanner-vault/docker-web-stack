#!/bin/bash

# TODO: Check the .env file exists

# TODO: For each of the values check it does not equal the placeholder (itself in %%)

#docker-compose -f 'stacks/portainer/docker-compose.yml' -p 'portainer' up -d --remove-orphans
#docker-compose -f 'stacks/mysql/docker-compose.yml' -p 'mysql' up -d --remove-orphans
#docker-compose -f 'stacks/redis/docker-compose.yml' -p 'redis' up -d --remove-orphans
#docker-compose -f 'stacks/mailhog/docker-compose.yml' -p 'mailhog' up -d --remove-orphans
#docker-compose -f 'stacks/mongo/docker-compose.yml' -p 'mongo' up -d --remove-orphans
